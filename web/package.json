{
  "name": "edge-viewer-web",
  "version": "1.0.0",
  "description": "Minimal TypeScript viewer for edge detection results.",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && open index.html" 
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}